import * as React from 'react';

export const Logo = ({ className }: { className?: string }) => (
    <img src="/images/varahe-logo.png" alt="Varahe Logo" width={40} height={40} className={className} />
);
